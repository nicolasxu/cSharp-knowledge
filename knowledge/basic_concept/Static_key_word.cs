using System;

namespace CSharpKnowledge
{
	public class Static_key_word
	{
		// 1. static members are scoped to the class level (rather than the object level) 
		// 2. and can thus be invoked without the need to first create a new class instance.

	}
}

