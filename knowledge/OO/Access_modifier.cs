using System;

namespace CSharpKnowledge
{
	public class Access_modifier
	{
		public Access_modifier ()
		{
			/*
			 * private - visible to the class member defines them only, no child class, no c
			 * protected - no outside access, its defiing class have access and child class access
			 * internal 
			 * projected internal
			 *
			 */
		}
	}
}

